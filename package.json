{
  "name": "twine-webbuild-template",
  "version": "1.0.0",
  "private": true,
  "description": "Build a Twine story as Windows EXE or Android APK with GitHub Actions.",
  "main": "builder/electron/main.cjs",
  "type": "module",
  "scripts": {
    "prepare:web": "node builder/prepare.mjs",
    "check": "node builder/prepare.mjs --check-only",
    "build:exe": "npm run prepare:web && node builder/protect-media.mjs && electron-builder --win nsis --x64 --publish never --config builder/electron-builder.cjs",
    "android:init": "npx cap add android",
    "android:configure": "node builder/configure-android.mjs",
    "android:sync": "npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/cli": "8.5.0",
    "@capacitor/core": "8.5.0",
    "electron": "43.4.1",
    "electron-builder": "26.15.3",
    "sharp": "0.35.3"
  }
}
